<h1>Missions#index</h1>
<div class="container-flex">
  <% @missions.each do |mission|%>
    <div class="item-flex" style="background-image: url('https://picsum.photos/300/200')">
        <h2><%= mission.name %></h2>
    </div>
  <% end %>
</div>
